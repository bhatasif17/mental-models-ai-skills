# Claude adapter

Copy the `skills/` folders into `.claude/skills/` or run `scripts/install_claude.sh` from the repository root.

Claude and Claude Code skill bundles use `SKILL.md` instruction files.
